import React from "react";

const Hero = () => {
  return <header className="bg-hero bg-no-repeat bg-center bg-cover h-screen">
    <div className="h-full bg-black bg-opacity-50 flex justify-center items-center">
      <a href="#" className="px-5 py-3 bg-gradient-to-r from-yellow-400 via-blue-300 to-green rounded-[32px] text-white font-bold">Lets Discover</a>
    </div>
  </header>;
};

export default Hero;
